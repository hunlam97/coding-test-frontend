import { Container } from "@chakra-ui/react";
import { Navigator } from "components";

const Home = () => {
  return <Container maxW="container.lg">Use the icon to navigate</Container>;
};

export default Home;
