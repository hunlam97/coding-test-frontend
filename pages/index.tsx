import { Container } from "@chakra-ui/react";
import { Navigator } from "components";

const Home = () => {
  return (
    <Container maxW="container.lg">
      <Navigator />
    </Container>
  );
};

export default Home;
